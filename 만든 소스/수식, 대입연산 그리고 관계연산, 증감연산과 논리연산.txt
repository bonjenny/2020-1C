#include <stdio.h>
int main(void)

{
int m,n; char x,y
printf("두 문자를 입력하세요.");
scanf("%c%c", &x&y);

printf("\n입력한 두 문자 %c, %c를 비교하면\n)");
prinff("\t(%c>%c)의 관계연산결과는 %d입니다.\n", x,y,x>y);
//맞으면 1, 틀리면 0

x=x++
y=y++

printf("논리연산 !(x!=y)||(x>y) 결과값은 %d", !(x!=y)||(x>y));
printf("논리연산 (x-y==1)&&(x+y==3) 결과값은 %d", (x-y==1)&&(x+y==3));

return 0;
}